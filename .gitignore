bin/
*.exe
*.test
*.out
*.log

# Never commit real credentials
config.json
config.local.json
.env
.env.local

# Local cache (per-device, lives in %LOCALAPPDATA% / ~/.local/share — never in the repo)
local.db
local.db-wal
local.db-shm
